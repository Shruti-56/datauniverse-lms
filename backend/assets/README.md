# Certificate assets

Place **certificate-logo.png** here (DataUniverse / institute logo) for use as watermark and header on generated certificates.

If this file is missing, the backend will also look for:
- `../public/institute-logo.png` (project root public folder)
- `../logo.png` (project root)

You can override the path with the `CERTIFICATE_LOGO_PATH` environment variable.
